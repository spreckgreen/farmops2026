# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Self-hosted Supabase env template — may contain real keys after fill script; never commit.
docs/env.self-hosted-supabase.example

# Local configuration safety copies.
Caddyfile.pre-*
docker-compose.yml.pre-*

# Local env overrides — hold real Supabase/service keys for `docker compose`
# (loaded via COMPOSE_ENV_FILES). Also matched by `*.local` above; kept
# explicit so the intent is obvious. Never commit.
.env.local
.env.*.local
.env.local.bak.*

# Generated by scripts/apply-migrations.sh --fix-sql
migration-remediation-*.sql
