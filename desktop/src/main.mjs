import { app, BrowserWindow, dialog, ipcMain } from "electron";
import fs from "node:fs"; import path from "node:path"; import crypto from "node:crypto"; import {fileURLToPath} from "node:url"; import { openProfileDatabase, DESKTOP_SCHEMA_VERSION } from "./local-storage.mjs"; import { buildDesktopHealthHtml } from "./health-screen.mjs"; import {configureBackupFolder} from "./backup-folder-setup.mjs"; import {createManualProfileBackup,loadBackupDestination} from "./manual-backup.mjs"; import {previewRestoreArchive} from "./restore-preview.mjs"; import {restoreProfileArchive} from "./restore-profile.mjs"; import {createRestoreSession,sameRestorePreview,toPublicRestorePreview} from "./restore-session.mjs"; import {getRecoveryHealth,recordRestoreEvent} from "./recovery-history.mjs"; import {cleanupRestoreRollbacks} from "./rollback-cleanup.mjs"; import {applyBackupRetention,planBackupRetention} from "./backup-retention.mjs"; import {loadBackupSchedule,saveBackupSchedule} from "./backup-schedule.mjs"; import {createScheduledBackupRunner,loadScheduledBackupStatus} from "./scheduled-backup-runner.mjs";

let profileDatabase;let backupTimer;
if (!app.requestSingleInstanceLock()) app.quit();
function ensureIdentity(root){const file=path.join(root,"installation.json");fs.mkdirSync(root,{recursive:true});if(!fs.existsSync(file))fs.writeFileSync(file,JSON.stringify({installationId:crypto.randomUUID(),deviceId:crypto.randomUUID(),createdAt:new Date().toISOString()},null,2));}
function registerDesktopHandlers(userData,opened,profileType){const performBackup=()=>{const site=opened.db.prepare("SELECT id FROM sites ORDER BY id LIMIT 1").get();return createManualProfileBackup({userDataRoot:userData,profile:opened.profile,profileId:profileType,profileType,siteId:site?.id??null,appVersion:app.getVersion(),schemaVersion:DESKTOP_SCHEMA_VERSION,checkpointDatabase:()=>opened.db.exec("PRAGMA wal_checkpoint(FULL)")});};const scheduledRunner=createScheduledBackupRunner({userDataRoot:userData,getSchedule:()=>loadBackupSchedule(userData),runBackup:performBackup});const restoreSession=createRestoreSession();let retentionPlan=null;const preview=archivePath=>previewRestoreArchive({archivePath,currentAppVersion:app.getVersion(),maxSchemaVersion:DESKTOP_SCHEMA_VERSION,targetProfileId:profileType});ipcMain.handle("farmops:select-backup-destination",async()=>{const selection=await dialog.showOpenDialog({title:"Select FarmOps backup destination",properties:["openDirectory","createDirectory"]});if(selection.canceled||selection.filePaths.length!==1)return {configured:false,canceled:true};try{return configureBackupFolder({userDataRoot:userData,directory:selection.filePaths[0]});}catch(error){return {configured:false,canceled:false,error:error instanceof Error?error.message:"Backup destination failed"};}});ipcMain.handle("farmops:select-restore-preview",async()=>{restoreSession.clear();const selection=await dialog.showOpenDialog({title:"Select FarmOps backup to verify",properties:["openFile"],filters:[{name:"FarmOps backup",extensions:["farmops-backup"]}]});if(selection.canceled||selection.filePaths.length!==1)return {valid:false,canceled:true};const result=preview(selection.filePaths[0]);restoreSession.record(result);return toPublicRestorePreview(result);});ipcMain.handle("farmops:confirm-restore",async()=>{const pending=restoreSession.get();if(!pending)return {ok:false,reason:"no-verified-backup"};const fresh=preview(pending.path);if(!sameRestorePreview(pending,fresh)){restoreSession.clear();return {ok:false,reason:fresh.valid?"backup-changed-since-verification":fresh.reason};}const choice=await dialog.showMessageBox({type:"warning",title:"Restore FarmOps backup?",message:`Replace the current ${profileType} profile with this verified backup?`,detail:`Backup: ${fresh.createdAt}\nSchema: ${fresh.schemaVersion}\nFarmOps will close and restart. Current profile data will be replaced.`,buttons:["Cancel","Restore and restart"],defaultId:0,cancelId:0,noLink:true});if(choice.response!==1)return {ok:false,canceled:true};restoreSession.clear();const restoreId=crypto.randomUUID();recordRestoreEvent(userData,{id:restoreId,status:"started",profileId:profileType,backupCreatedAt:fresh.createdAt});try{opened.db.exec("PRAGMA wal_checkpoint(FULL)");opened.db.close();profileDatabase=undefined;const restored=restoreProfileArchive({archivePath:fresh.path,targetProfileRoot:opened.profile.root,currentAppVersion:app.getVersion(),maxSchemaVersion:DESKTOP_SCHEMA_VERSION,targetProfileId:profileType});recordRestoreEvent(userData,{id:restoreId,status:"succeeded",profileId:profileType,backupCreatedAt:fresh.createdAt,cleanupRequired:restored.cleanupRequired,rollbackPath:restored.rollbackPath});app.relaunch();app.exit(0);return {ok:true,restarting:true};}catch(error){recordRestoreEvent(userData,{id:restoreId,status:"failed",profileId:profileType,backupCreatedAt:fresh.createdAt,reason:error instanceof Error?error.message:"Restore failed"});await dialog.showMessageBox({type:"error",title:"FarmOps restore failed",message:"The restore could not be completed.",detail:error instanceof Error?error.message:"Restore failed"});app.relaunch();app.exit(1);return {ok:false,reason:"restore-failed"};}});ipcMain.handle("farmops:cleanup-restore-rollbacks",async()=>{const pending=getRecoveryHealth(userData).cleanup;if(pending.length===0)return {ok:true,removedCount:0};const choice=await dialog.showMessageBox({type:"warning",title:"Remove restore rollback data?",message:`Permanently remove ${pending.length} obsolete rollback folder(s)?`,detail:"The active FarmOps profile and backup archives will not be removed. This action cannot be undone.",buttons:["Cancel","Remove rollback data"],defaultId:0,cancelId:0,noLink:true});if(choice.response!==1)return {ok:false,canceled:true};try{return cleanupRestoreRollbacks(userData);}catch(error){return {ok:false,reason:error instanceof Error?error.message:"Rollback cleanup failed"};}});ipcMain.handle("farmops:preview-backup-retention",()=>{retentionPlan=null;try{const destination=loadBackupDestination(userData);retentionPlan=planBackupRetention({directory:destination.directory,profileId:profileType,keep:10});return {ok:true,keep:retentionPlan.keep,protectedCount:retentionPlan.protected.length,removableCount:retentionPlan.removable.length,invalidCount:retentionPlan.invalid.length};}catch(error){return {ok:false,reason:error instanceof Error?error.message:"Retention preview failed"};}});ipcMain.handle("farmops:apply-backup-retention",async()=>{if(!retentionPlan)return {ok:false,reason:"no-retention-preview"};const plan=retentionPlan;const choice=await dialog.showMessageBox({type:"warning",title:"Remove old verified backups?",message:`Permanently remove ${plan.removable.length} old verified backup(s)?`,detail:`The newest ${plan.keep} verified backups for this profile will be kept. Invalid archives and other profiles are not removed.`,buttons:["Cancel","Remove old backups"],defaultId:0,cancelId:0,noLink:true});if(choice.response!==1)return {ok:false,canceled:true};retentionPlan=null;try{return applyBackupRetention(plan);}catch(error){return {ok:false,reason:error instanceof Error?error.message:"Backup retention failed"};}});ipcMain.handle("farmops:backup-schedule-status",()=>({schedule:loadBackupSchedule(userData),status:loadScheduledBackupStatus(userData)}));ipcMain.handle("farmops:set-backup-schedule",(_event,enabled)=>{if(typeof enabled!=="boolean")return {ok:false,reason:"invalid-enabled-state"};const current=loadBackupSchedule(userData);const schedule=saveBackupSchedule(userData,{...current,enabled});if(enabled)void scheduledRunner.tick();return {ok:true,schedule,status:loadScheduledBackupStatus(userData)};});ipcMain.handle("farmops:backup-now",()=>{try{return {ok:true,...performBackup()};}catch(error){return {ok:false,error:error instanceof Error?error.message:"Backup failed"};}});return scheduledRunner;}
function createWindow(){const userData=app.getPath("userData");ensureIdentity(userData);const profileType=process.env.FARMOPS_PROFILE??"demo";const opened=openProfileDatabase(userData,profileType);const scheduledRunner=registerDesktopHandlers(userData,opened,profileType);profileDatabase=opened.db;void scheduledRunner.tick();backupTimer=setInterval(()=>void scheduledRunner.tick(),60000);const window=new BrowserWindow({width:1440,height:900,webPreferences:{contextIsolation:true,sandbox:true,nodeIntegration:false,preload:path.join(path.dirname(fileURLToPath(import.meta.url)),"preload.cjs")}});const url=process.env.FARMOPS_DESKTOP_URL;if(url)window.loadURL(url);else window.loadURL("data:text/html;charset=utf-8,"+encodeURIComponent(buildDesktopHealthHtml({profileType,schemaVersion:DESKTOP_SCHEMA_VERSION,databasePath:opened.profile.database,appVersion:app.getVersion(),recoveryHealth:getRecoveryHealth(userData)})));}
app.whenReady().then(createWindow);app.on("before-quit",()=>{if(backupTimer)clearInterval(backupTimer);backupTimer=undefined;profileDatabase?.close();profileDatabase=undefined;});app.on("window-all-closed",()=>{if(process.platform!=="darwin")app.quit();});
