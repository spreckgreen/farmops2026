function escape(value){return String(value).replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));}
export function buildDesktopHealthHtml({profileType,schemaVersion,databasePath,appVersion,backupConfigured=false,aiConfigured=false,licenseStatus="demo-trial",recoveryHealth={history:[],cleanupRequired:false,cleanup:[]}}){const latestRestore=recoveryHealth.history?.[0]??null;const warnings=[recoveryHealth.cleanupRequired&&{label:"Restore rollback cleanup is required",target:"#recovery"},!backupConfigured&&{label:"Backup destination is not configured",target:"#config-backup"},!aiConfigured&&{label:"AI provider is not configured",target:"#config-ai"}].filter(Boolean);const status=warnings.length?"Setup needs attention":"Ready";return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>FarmOps Desktop Health</title><style>:root{font-family:system-ui;color:#183126;background:#f3f6f2}body{margin:0}header{background:#244d37;color:white;padding:24px 32px}main{max-width:1100px;margin:auto;padding:24px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:16px}.card{background:white;border:1px solid #ccd8cf;border-radius:12px;padding:18px}.ok{color:#176b3a}.warn{color:#925f00}a{color:#145b37}.value{font-weight:700;word-break:break-word}button{padding:9px 12px}</style></head><body><header><h1>FarmOps Desktop</h1><p>Offline health and setup</p></header><main><h2 class="${warnings.length?"warn":"ok"}">${status}</h2><section class="grid"><article class="card"><h3>Profile</h3><p class="value">${escape(profileType)}</p><p>Database schema ${escape(schemaVersion)}</p><p>${escape(databasePath)}</p></article><article class="card"><h3>Application</h3><p>Version <span class="value">${escape(appVersion)}</span></p><p>License: <span class="value">${escape(licenseStatus)}</span></p><a href="#subscription">Subscription &amp; modules</a></article><article class="card"><h3>Configuration health</h3>${warnings.length?`<ul>${warnings.map(item=>`<li>⚠ <a href="${item.target}">${escape(item.label)}</a></li>`).join("")}</ul>`:"<p class=\"ok\">✓ Required setup is ready</p>"}</article></section><section class="grid" style="margin-top:16px"><article class="card" id="config-backup"><h3>Backup</h3><p>${backupConfigured?"Configured":"Choose a local, mounted file-server, OneDrive, or Google Drive synchronized folder."}</p><button id="select-backup" type="button">Select backup destination</button> <button id="backup-now" type="button">Back up now</button> <button id="restore-preview" type="button">Select and verify backup</button> <button id="restore-confirm" type="button" disabled>Restore verified backup</button><p id="backup-result" aria-live="polite"></p></article><article class="card" id="recovery"><h3>Recovery history</h3>${recoveryHealth.cleanupRequired?`<p class="warn">⚠ ${escape(recoveryHealth.cleanup.length)} rollback folder(s) require cleanup.</p>`:""}${latestRestore?`<p>Latest restore: <strong>${escape(latestRestore.status)}</strong></p><p>${escape(latestRestore.recordedAt)}</p>`:"<p>No restore attempts recorded.</p>"}</article><article class="card" id="config-ai"><h3>AI provider</h3><p>${aiConfigured?"Configured":"AI is optional. Configure BYOK, Ollama/local, or leave AI disabled."}</p><button type="button">Configure AI</button></article><article class="card" id="subscription"><h3>Subscription &amp; modules</h3><p>Procedures: <strong>Free for life</strong></p><p>Paid modules remain visible and retain their data when locked.</p></article></section></main><script>document.getElementById("select-backup").addEventListener("click",async()=>{const output=document.getElementById("backup-result");if(!window.farmopsDesktop){output.textContent="Desktop setup service is unavailable.";return;}output.textContent="Checking destination…";const result=await window.farmopsDesktop.selectBackupDestination();if(result.configured){output.textContent="✓ Backup destination ready: "+result.destination.directory;}else if(result.canceled){output.textContent="No folder selected.";}else{output.textContent="⚠ "+result.error;}});document.getElementById("backup-now").addEventListener("click",async()=>{const output=document.getElementById("backup-result");output.textContent="Creating and verifying backup…";const result=await window.farmopsDesktop?.backupNow();output.textContent=result?.ok?"✓ Verified backup: "+result.path+" ("+result.verification.valid+")":"⚠ "+(result?.error??"Desktop backup service is unavailable.");});document.getElementById("restore-preview").addEventListener("click",async()=>{const output=document.getElementById("backup-result");output.textContent="Reading and verifying backup…";const result=await window.farmopsDesktop?.selectRestorePreview();const restoreButton=document.getElementById("restore-confirm");restoreButton.disabled=true;if(result?.canceled)output.textContent="No backup selected.";else if(result?.valid){restoreButton.disabled=false;output.textContent="✓ Compatible backup from "+result.createdAt+"; schema "+result.schemaVersion+"; "+result.fileCount+" files. No data has been changed.";}else output.textContent="⚠ Backup cannot be restored: "+(result?.reason??"Desktop restore service is unavailable.");});document.getElementById("restore-confirm").addEventListener("click",async()=>{const output=document.getElementById("backup-result");output.textContent="Waiting for restore confirmation…";const result=await window.farmopsDesktop?.confirmRestore();if(result?.canceled)output.textContent="Restore canceled. No data was changed.";else if(!result?.ok)output.textContent="⚠ Restore not started: "+(result?.reason??"Desktop restore service is unavailable.");});</script></body></html>`;}
